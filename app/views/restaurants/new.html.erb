<h1>Add a new Restaurant</h1>

<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :food %>
  <%= f.input :description %>
  <%= f.input :location %>
  <%= f.button :submit %>
 <% end %>
